// import React from "react";
// import { act } from "react-test-renderer";
// import { render, screen } from "@testing-library/react";
// import "@testing-library/jest-dom";
// import { MemoryRouter } from "react-router-dom";
// import ItemContainers from "../shop/ItemContainer";

beforeEach(() => {});

describe("ItemContainers component", () => {
  it.skip("", () => {});
  // it("ItemContainer contains Navbar and Footer", () => {
  //   const navbar = screen.getByRole("navigation");
  //   const footer = screen.getByRole("contentinfo", { name: "footer" });
  //   expect(navbar).toBeInTheDocument();
  //   expect(footer).toBeInTheDocument();
  // });
  // it("ItemContainer component always renders with Home link in Navbar", () => {
  //   const navbar = screen.getByRole("navigation");
  //   const home = screen.getByRole("link", { name: "home" });
  //   expect(navbar).toContainElement(home);
  // });
});
